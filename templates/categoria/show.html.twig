{% extends 'base.html.twig' %}

{% block title %}Categoria{% endblock %}

{% block body %}
    <h1>Categoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorium.id }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ categorium.categoria }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary btn-outline-light" href="{{ path('app_categoria_index') }}">back to list</a>

    <a class="btn btn-warning btn-outline-light" href="{{ path('app_categoria_edit', {'id': categorium.id}) }}">edit</a>

    {{ include('categoria/_delete_form.html.twig') }}
{% endblock %}
